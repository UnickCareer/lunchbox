# URL encoded in the login screen's QR code.
# Set this to your deployed link (Vercel/Netlify/etc.) so phones scanning
# the QR code land on the right site. Falls back to the current page URL
# in local dev if left unset.
VITE_APP_URL=https://your-deployed-url.example.com
